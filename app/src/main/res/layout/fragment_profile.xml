<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- 1. APP BAR LAYOUT -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true">

        <!-- 🔸 КОЛЛАПСИНГ (Аватарка) 🔸 -->
        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsingToolbar"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:contentScrim="?attr/colorSurface"
            app:titleEnabled="false"
            app:layout_collapseParallaxMultiplier="1"> <!-- Синхронизация движения -->

            <!-- 🔹 ImageView 🔸 -->
            <ImageView
                android:id="@+id/imgAvatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitCenter"
                android:src="@drawable/avatar"
                app:layout_collapseMode="parallax"
                android:paddingTop="?attr/actionBarSize"/> <!-- Отступ = высота верхней панели -->

        </com.google.android.material.appbar.CollapsingToolbarLayout>

        <!-- ТАБЫ -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabsWallJobs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorSurface"
            app:tabGravity="fill"
            app:tabIndicatorColor="?attr/colorPrimary"
            app:tabInlineLabel="true"
            app:tabMode="fixed"
            app:tabSelectedTextColor="?attr/colorPrimary"
            app:tabTextColor="#B3B3B3"
            app:layout_scrollFlags="scroll|enterAlways"/>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- СТАТИЧНАЯ ПАНЕЛЬ (Назад, Имя, Выход) -->
    <LinearLayout
        android:id="@+id/staticTopPanel"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorSurface"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:layout_gravity="top"
        app:elevation="8dp">

        <!-- КНОПКА НАЗАД -->
        <ImageButton
            android:id="@+id/btnBack"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Назад"
            android:src="@drawable/back" />

        <!-- ИМЯ -->
        <TextView
            android:id="@+id/tvHeaderName"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Livia Dokidis"
            android:textStyle="bold"
            android:textSize="16sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:textColor="?attr/colorOnSurface"/>

        <!-- КНОПКА ВЫХОД -->
        <ImageButton
            android:id="@+id/btnLogout"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Выход"
            android:src="@drawable/exit_to_app_24dp_000000_fill0_wght400_grad0_opsz24" />

    </LinearLayout>

    <!-- КОНТЕНТ -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPagerWallJobs"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>