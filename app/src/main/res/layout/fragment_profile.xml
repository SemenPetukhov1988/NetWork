<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Верхняя панель -->
    <LinearLayout
        android:id="@+id/topBarProfile"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#CEDFE8"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Кнопка назад -->
        <ImageButton
            android:id="@+id/btnBack"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:elevation="0dp"

            android:src="@drawable/back" />


        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Livia Dokidis / togilbyct"
            android:textSize="18sp"

            android:textStyle="bold" />

        <!-- 👉 Новая кнопка выхода без стиля Material -->
        <ImageButton
            android:id="@+id/btnLogout"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:elevation="0dp"
            android:iconTint="#7A7D8C"
            android:src="@drawable/exit_to_app_24dp_000000_fill0_wght400_grad0_opsz24" />

    </LinearLayout>

    <!-- Аватарка -->
    <ImageView
        android:id="@+id/imgAvatar"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:scaleType="centerCrop"
        android:src="@drawable/avatar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/topBarProfile" />

    <!-- ⚡️ Вкладки Wall/Jobs — теперь только системные стили! -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabsWallJobs"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#CEDFE8"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imgAvatar"></com.google.android.material.tabs.TabLayout>


    <!-- Контейнер для дочерних фрагментов -->
    <!-- ⚡️ ДОБАВЛЕН БЕЛЫЙ ФОН! Это решает проблему пустого экрана -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragmentContainerWallJob"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tabsWallJobs">

    </androidx.fragment.app.FragmentContainerView>

</androidx.constraintlayout.widget.ConstraintLayout>