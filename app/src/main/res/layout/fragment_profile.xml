<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- APP BAR LAYOUT: fitsSystemWindows=false, чтобы не дублировать отступы -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="false"
        app:liftOnScroll="true">

        <!-- COLLAPSING TOOLBAR:
             contentInsetStart/End = 0dp убирает внутренние отступы, из-за которых был лишний воздух.
             fitsSystemWindows=false нужен, чтобы не было двойного отступа под статус-бар. -->
        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsingToolbar"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:fitsSystemWindows="false"
            app:contentInsetStart="0dp"
            app:contentInsetEnd="0dp"
            app:contentScrim="?attr/colorSurface"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:titleEnabled="false">

            <!-- АВАТАРКА:
                 layout_gravity="center" ставит картинку ровно по центру контейнера.
                 Лишние края обрезаются, но центр остается на месте. -->
            <ImageView
                android:id="@+id/imgAvatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:src="@drawable/avatar"
                app:layout_collapseMode="parallax" />

        </com.google.android.material.appbar.CollapsingToolbarLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- ПАНЕЛЬ С ИМЕНЕМ: Статичная, поверх всего -->
    <LinearLayout
        android:id="@+id/staticTopPanel"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorSurface"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:elevation="8dp">

        <ImageButton
            android:id="@+id/btnBack"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Назад"
            android:src="@drawable/back" />

        <TextView
            android:id="@+id/tvHeaderName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:text="Livia Dokidis"
            android:textColor="?attr/colorOnSurface"
            android:textSize="16sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/btnLogout"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Выход"
            android:src="@drawable/exit_to_app_24dp_000000_fill0_wght400_grad0_opsz24" />
    </LinearLayout>

    <!-- КОНТЕЙНЕР ДЛЯ ТАБОВ И КОНТЕНТА -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- ТАБЫ:
             marginTop=?attr/actionBarSize нужен, чтобы табы встали ровно под панель с именем.
             Не меняй это значение — отступ между панелью и табами должен быть равен высоте панели. -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabsWallJobs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorSurface"
            android:layout_marginTop="?attr/actionBarSize"
            app:tabGravity="fill"
            app:tabIndicatorColor="?attr/colorPrimary"
            app:tabInlineLabel="true"
            app:tabMode="fixed"
            app:tabSelectedTextColor="?attr/colorPrimary"
            app:tabTextColor="#B3B3B3" />

        <!-- ОСНОВНОЙ КОНТЕНТ (Лента) -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/fragmentContainerWallJob"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
